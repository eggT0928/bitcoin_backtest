{
  "name": "btc-telegram-alerts",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "lint": "node --check index.js && node --check signal-engine.js",
    "test": "node --test test/*.test.js",
    "test:parity": "python test/app-position-parity.py"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0"
  }
}
